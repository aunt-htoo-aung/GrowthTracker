---
trigger: always_on
---
When modifying any API endpoint, request/response structure, or authentication flow in the backend, also update `API_DOCUMENTATION.md` to reflect the changes. This includes adding new endpoints, changing request/response schemas, updating query parameters, or modifying the authentication mechanism. The documentation must always match the current backend implementation.
